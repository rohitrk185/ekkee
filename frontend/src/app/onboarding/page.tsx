import React from "react";

// Implement SSG and pass it to the component below
type Props = {};

const page = (props: Props) => {
  return <div className="text-xl text-blue-500">page</div>;
};

export default page;
